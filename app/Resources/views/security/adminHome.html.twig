{% extends 'base.html.twig' %}
{% block title %}Staff homepage{% endblock %}
{% block body %}
    <h1 class="authWarning"> *** WARNING: AUTHORISED ACCESS ONLY ***</h1>
    {% if not is_granted('ROLE_STAFF') %}{#only show this option if staff user#}
        <p class="authWarning">Please <a href="{{ path('login') }}">login</a> to access.</p>
    {% endif %}
    {% if is_granted('ROLE_STAFF') %}{#only show this option if staff user#}
        <ul class="centre">
            <li><a href="{{ path('user_index') }}">list of users</a></li>
            <li><a href="{{ path('book_index') }}">list of books</a></li>
            <li><a href="{{ path('loans_index') }}">list of loans</a></li>
            <li><a href="{{ path('my_loans') }}">MyLoans page</a></li>
        </ul>

    {% endif %}
{% endblock %}